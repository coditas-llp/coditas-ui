export default interface IImageTextComponent {
    firstImage?: string;
    secondImage?: string;
    subtext: string;
    heading: string;
    reverseCard: boolean;
    firstImageAlt: string;
    secondImageAlt: string;
}
